1. If we use ...params , we will get the array includes all parameters <- although we pass comma sepreated arguments
        implement -> authorizeRoles(...roles)

2. Calling the API sequence matters a lot , scpecially when case of params in the api
    -> /all is also understand as the params (/category/:id) , so be caution
        implement ->  getCategories & getCategory API calling

3. how to get the host url - req.get("host")
    implement -> forgetPassword controller

4. if reset password link is not sent , just undefined the resetPasswordToken & expire in user <- for security
    implement -> forgetPassword controller


5. set the db fields - as undefined & null -> what is difference { read in deapth }

6. Empty array treated as true in just
        implement -> getAllPosts controller